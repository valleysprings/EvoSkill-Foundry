# co-bench

External CO-Bench task wired to the official evaluator.

The checked-in config starts with a small curated subset so that the task is runnable without immediately paying the full 36-task cost. Increase `problem_names` or use `--max-items` once the path is stable.
